% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-validation.R
\name{validate_survey_data}
\alias{validate_survey_data}
\title{Validate Survey Data for GMM Analysis}
\usage{
validate_survey_data(
  data,
  id,
  time,
  outcome,
  strata = NULL,
  cluster = NULL,
  weights = NULL,
  covariates = NULL,
  min_observations = 2,
  check_weights = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame in long format}

\item{id}{Character string naming ID variable}

\item{time}{Character string naming time variable}

\item{outcome}{Character string naming outcome variable}

\item{strata}{Optional character string naming stratification variable}

\item{cluster}{Optional character string naming cluster/PSU variable}

\item{weights}{Optional character string naming weight variable}

\item{covariates}{Optional character vector naming covariate variables}

\item{min_observations}{Minimum number of observations per individual (default = 2)}

\item{check_weights}{Logical; perform detailed weight diagnostics?}

\item{verbose}{Logical; print detailed output?}
}
\value{
An S4 object of class \code{DataValidation} with slots:
  \item{is_valid}{Logical; overall validation status}
  \item{errors}{Character vector of critical errors}
  \item{warnings}{Character vector of warnings}
  \item{info}{List of diagnostic information}
  \item{recommendations}{Character vector of recommended actions}
}
\description{
Comprehensive validation of data structure, survey design elements, and
suitability for growth mixture modeling. Provides detailed diagnostics
and warnings about potential issues.
}
\details{
The validation checks include:

**Data Structure:**
- All required variables present
- Correct data types
- No duplicate ID-time combinations
- Sufficient observations per individual

**Survey Design:**
- Clusters nested within strata
- Weights are positive and non-missing
- Suspicious weight patterns (e.g., all identical)
- Design effects

**Missingness:**
- Missing data patterns (MCAR, MAR, MNAR indicators)
- Proportion missing by variable
- Monotone vs non-monotone patterns

**Statistical Properties:**
- Outcome distribution (normality, outliers)
- Time score distribution
- Covariate distributions
- Multicollinearity among covariates
}
\examples{
\donttest{
data(mcs_simulated)

# Validate data before analysis
validation <- validate_survey_data(
  data = mcs_simulated,
  id = "id",
  time = "age",
  outcome = "selfcontrol",
  strata = "stratum",
  cluster = "cluster",
  weights = "weight",
  verbose = TRUE
)

print(validation)

# Check if data is valid for analysis
if (validation@is_valid) {
  cat("Data is ready for GMM analysis\n")
} else {
  cat("Please address errors before proceeding:\n")
  cat(validation@errors, sep = "\n")
}
}
}
