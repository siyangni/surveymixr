% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lmr-test.R
\name{lmr_sequential}
\alias{lmr_sequential}
\title{Automated LMR Testing Across Multiple Models}
\usage{
lmr_sequential(model_list, adjusted = TRUE)
}
\arguments{
\item{model_list}{List of fitted \code{SurveyMixr} objects with increasing
number of classes}

\item{adjusted}{Logical; use adjusted LMR?}
}
\value{
Data frame with LMR test results for each comparison
}
\description{
Performs sequential LMR tests to compare models with 1 to K classes.
This is integrated into \code{\link{gmm_select}} but can be called separately.
}
\examples{
\donttest{
# Fit models with 1-4 classes (slow - use for actual analysis)
data(mcs_simulated)
models <- lapply(1:4, function(k) {
  gmm_survey(
    data = mcs_simulated,
    id = "id", time = "age", outcome = "selfcontrol",
    n_classes = k, starts = 50
  )
})

# Run LMR tests
lmr_results <- lmr_sequential(models, adjusted = TRUE)
print(lmr_results)
}
}
