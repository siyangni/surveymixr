% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcome-types.R
\name{gmm_survey_ordinal}
\alias{gmm_survey_ordinal}
\title{Growth Mixture Model for Ordinal Outcomes}
\usage{
gmm_survey_ordinal(
  data,
  id,
  time,
  outcome,
  n_classes,
  n_categories,
  growth_model = "linear",
  strata = NULL,
  cluster = NULL,
  weights = NULL,
  covariates = NULL,
  ordinal_model = c("proportional_odds", "adjacent_category"),
  starts = 500,
  cores = parallel::detectCores() - 1,
  convergence_threshold = 1e-06,
  max_iterations = 1000,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame in long format containing repeated measurements}

\item{id}{Character string specifying the individual ID variable name}

\item{time}{Character string specifying the time variable name}

\item{outcome}{Character string specifying the outcome variable name}

\item{n_classes}{Integer number of latent classes (default: 2)}

\item{n_categories}{Integer specifying number of ordered categories}

\item{growth_model}{Character string specifying growth model type:
\code{"linear"} (default), \code{"quadratic"}, \code{"nonlinear"},
or \code{"free_basis"}}

\item{strata}{Character string specifying stratification variable (optional)}

\item{cluster}{Character string specifying cluster/PSU variable (optional)}

\item{weights}{Character string specifying sampling weights variable (optional)}

\item{covariates}{Character vector of time-varying covariate names (not yet implemented)}

\item{ordinal_model}{Character: "proportional_odds" (default) or "adjacent_category"}

\item{starts}{Integer number of random starts (default: 500)}

\item{cores}{Integer number of CPU cores for parallel processing (default: 1)}

\item{convergence_threshold}{Numeric convergence criterion for EM algorithm (default: 1e-6)}

\item{max_iterations}{Integer maximum number of EM iterations (default: 1000)}

\item{seed}{Integer random seed for reproducibility (optional)}

\item{verbose}{Logical, print progress messages? (default: TRUE)}
}
\value{
S4 object of class \code{SurveyMixr} with ordinal-specific components
}
\description{
Fits growth mixture models with ordinal outcomes using proportional odds
or adjacent category models within each latent class.
}
\details{
For ordinal outcomes with K categories (0, 1, ..., K-1), the proportional
odds model uses:
\deqn{P(Y_{it} \leq j | C_i = k) = \Phi(\tau_j - \eta_{itk})}
where \eqn{\tau_1 < \tau_2 < ... < \tau_{K-1}} are threshold parameters
and \eqn{\eta_{itk}} is the linear predictor.
}
\examples{
\donttest{
# Simulate ordinal outcome (e.g., Likert scale: 0-4)
ordinal_data <- simulate_gmm_survey(
  n_individuals = 1000,
  n_times = 4,
  n_classes = 3,
  outcome_type = "ordinal",
  n_categories = 5,
  design = "srs"
)

# Fit 3-class ordinal model
fit_ordinal <- gmm_survey_ordinal(
  data = ordinal_data,
  id = "id",
  time = "time",
  outcome = "outcome",
  n_classes = 3,
  n_categories = 5,
  ordinal_model = "proportional_odds",
  starts = 200
)

summary(fit_ordinal)
}
}
