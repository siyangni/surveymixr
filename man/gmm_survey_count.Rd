% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcome-types.R
\name{gmm_survey_count}
\alias{gmm_survey_count}
\title{Growth Mixture Model for Count Outcomes}
\usage{
gmm_survey_count(
  data,
  id,
  time,
  outcome,
  n_classes,
  growth_model = "linear",
  strata = NULL,
  cluster = NULL,
  weights = NULL,
  covariates = NULL,
  count_model = c("poisson", "negative_binomial", "zero_inflated"),
  offset = NULL,
  starts = 500,
  cores = parallel::detectCores() - 1,
  convergence_threshold = 1e-06,
  max_iterations = 1000,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame in long format containing repeated measurements}

\item{id}{Character string specifying the individual ID variable name}

\item{time}{Character string specifying the time variable name}

\item{outcome}{Character string specifying the outcome variable name}

\item{n_classes}{Integer number of latent classes (default: 2)}

\item{growth_model}{Character string specifying growth model type:
\code{"linear"} (default), \code{"quadratic"}, \code{"nonlinear"},
or \code{"free_basis"}}

\item{strata}{Character string specifying stratification variable (optional)}

\item{cluster}{Character string specifying cluster/PSU variable (optional)}

\item{weights}{Character string specifying sampling weights variable (optional)}

\item{covariates}{Character vector of time-varying covariate names (not yet implemented)}

\item{count_model}{Character: "poisson" (default), "negative_binomial", or "zero_inflated"}

\item{offset}{Optional offset variable (e.g., log exposure time)}

\item{starts}{Integer number of random starts (default: 500)}

\item{cores}{Integer number of CPU cores for parallel processing (default: 1)}

\item{convergence_threshold}{Numeric convergence criterion for EM algorithm (default: 1e-6)}

\item{max_iterations}{Integer maximum number of EM iterations (default: 1000)}

\item{seed}{Integer random seed for reproducibility (optional)}

\item{verbose}{Logical, print progress messages? (default: TRUE)}
}
\value{
S4 object of class \code{SurveyMixr} with count-specific components
}
\description{
Fits growth mixture models with count outcomes using Poisson or negative
binomial regression within each latent class.
}
\details{
For count outcomes, the model uses:
\deqn{Y_{it} | C_i = k \sim Poisson(\lambda_{itk})}
where \eqn{\log(\lambda_{itk}) = \eta_{itk}} and
\eqn{\eta_{itk}} includes growth parameters.

For zero-inflated models:
\deqn{P(Y_{it} = 0 | C_i = k) = \pi_k + (1 - \pi_k) \times e^{-\lambda_{itk}}}
\deqn{P(Y_{it} = y | C_i = k) = (1 - \pi_k) \times Poisson(y; \lambda_{itk}), \quad y > 0}
}
\examples{
\dontrun{
##D # Simulate count outcome data (e.g., number of delinquent acts)
##D count_data <- simulate_gmm_survey(
##D   n_individuals = 1500,
##D   n_times = 6,
##D   n_classes = 3,
##D
##D   design = "cluster",
##D   seed = 456
##D )
##D
##D # Convert continuous outcome to count data (non-negative integers)
##D # Shift to be positive then take absolute value and round to integers
##D count_data$outcome <- as.integer(round(abs(count_data$outcome + 3)))
##D
##D # Fit 3-class Poisson model
##D fit_count <- gmm_survey_count(
##D   data = count_data,
##D   id = "id",
##D   time = "time",
##D   outcome = "outcome",
##D   n_classes = 3,
##D   cluster = "cluster",
##D   weights = "weight",
##D   starts = 200
##D )
##D
##D summary(fit_count)
}
}
