% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm-survey.R
\name{gmm_survey}
\alias{gmm_survey}
\title{Growth Mixture Models with Complex Survey Design}
\usage{
gmm_survey(
  data,
  id,
  time,
  outcome,
  n_classes = 2,
  growth_model = "linear",
  strata = NULL,
  cluster = NULL,
  weights = NULL,
  nest = TRUE,
  outcome_type = "continuous",
  fixed_variances = FALSE,
  fixed_timescores = TRUE,
  predictors = NULL,
  distal = NULL,
  auxiliary = NULL,
  starts = 500,
  convergence = 1e-06,
  max_iter = 1000,
  cores = 1,
  verbose = TRUE,
  keep_data = FALSE,
  ...
)
}
\arguments{
\item{data}{Data frame in long format containing repeated measurements}

\item{id}{Character string specifying the individual ID variable name}

\item{time}{Character string specifying the time variable name}

\item{outcome}{Character string specifying the outcome variable name}

\item{n_classes}{Integer number of latent classes (default: 2)}

\item{growth_model}{Character string specifying growth model type:
\code{"linear"} (default), \code{"quadratic"}, \code{"nonlinear"},
or \code{"free_basis"}}

\item{strata}{Character string specifying stratification variable (optional)}

\item{cluster}{Character string specifying cluster/PSU variable (optional)}

\item{weights}{Character string specifying sampling weights variable (optional)}

\item{nest}{Logical indicating nested clustering (default: TRUE)}

\item{outcome_type}{Character string: \code{"continuous"} (default),
\code{"count"}, \code{"binary"}, or \code{"ordinal"}}

\item{fixed_variances}{Logical, should residual variances be constrained
equal across classes? (default: FALSE)}

\item{fixed_timescores}{Logical, should time scores be equal across classes?
(default: TRUE)}

\item{predictors}{Character vector of time-varying predictor variable names
(not yet implemented)}

\item{distal}{Character vector of distal outcome variable names (use with
\code{r3step()} function)}

\item{auxiliary}{Character vector of auxiliary variable names for class
membership prediction}

\item{starts}{Integer number of random starts (default: 500)}

\item{convergence}{Numeric convergence criterion (default: 1e-6)}

\item{max_iter}{Integer maximum number of EM iterations (default: 1000)}

\item{cores}{Integer number of CPU cores for parallel processing (default: 1)}

\item{verbose}{Logical, print progress messages? (default: TRUE)}

\item{keep_data}{Logical, store data in output object? (default: FALSE)}

\item{...}{Additional arguments (reserved for future use)}
}
\value{
An S4 object of class \code{\linkS4class{SurveyMixr}} containing:
\describe{
\item{parameters}{Class-specific growth parameters (means, variances)}
\item{class_proportions}{Weighted and unweighted class proportions}
\item{posterior_probs}{Posterior probabilities for each individual}
\item{standard_errors}{Survey-adjusted standard errors}
\item{fit_indices}{AIC, BIC, aBIC, entropy, log-likelihood}
\item{convergence_info}{Convergence diagnostics from random starts}
\item{survey_design}{Survey design information}
}
}
\description{
Fits growth mixture models for longitudinal data with full integration of
complex survey design features including stratification, clustering (PSUs),
and probability weights. Implements the EM algorithm with sandwich standard
errors for survey-adjusted inference.
}
\details{
This function implements growth mixture modeling with full survey design
integration, a feature combination not available in other R packages. The
implementation uses:
\itemize{
\item EM algorithm with survey weight integration in the likelihood
\item Multiple random starts to avoid local maxima
\item Sandwich standard errors for stratification and clustering
\item FIML for missing data
}

The function automatically converts data from long to wide format internally
for computational efficiency. Survey weights are normalized to sum to the
sample size.

For model selection across different numbers of classes, see
\code{\link{gmm_select}}. For analyzing auxiliary variables with
classification uncertainty correction, see \code{\link{r3step}}.
}
\examples{
\dontrun{
# Load simulated data
data(mcs_simulated)

# Fit 3-class model with survey design
fit <- gmm_survey(
  data = mcs_simulated,
  id = "id",
  time = "age",
  outcome = "selfcontrol",
  n_classes = 3,
  growth_model = "linear",
  strata = "stratum",
  cluster = "psu",
  weights = "weight",
  starts = 500,
  cores = 4
)

# View results
summary(fit)
plot(fit, type = "trajectories")

# Check convergence
diagnose_convergence(fit)
}

}
\references{
Muthén, B. (2004). Latent variable analysis: Growth mixture modeling and
related techniques for longitudinal data. In D. Kaplan (Ed.),
\emph{Handbook of quantitative methodology for the social sciences}
(pp. 345-368). Sage Publications.

Asparouhov, T., & Muthén, B. (2014). Auxiliary variables in mixture modeling:
Three-step approaches using Mplus. \emph{Structural Equation Modeling:
A Multidisciplinary Journal, 21}(3), 329-341.

Lumley, T. (2010). \emph{Complex surveys: A guide to analysis using R}.
John Wiley & Sons.
}
